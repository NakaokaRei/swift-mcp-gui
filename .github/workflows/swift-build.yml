name: Swift Build

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  build:
    name: Build
    runs-on: macos-15
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Show Swift version
      run: swift --version
    
    - name: Build Debug
      run: swift build -v
    
    - name: Build Release
      run: swift build -c release -v